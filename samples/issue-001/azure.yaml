# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: cheat-code-pdf-agent
metadata:
  template: cheat-code-issue-001

services:
  api:
    project: ./src
    language: py
    host: function

infra:
  provider: bicep
  path: ./infra
